# CLAUDE.md — PilotGear EG Store + Admin

Project memory for the PilotGear EG e-commerce site and Shopify-style admin.
Read this first before working on this project.

## What this is
A complete storefront **and** admin dashboard for **PilotGear EG**, an Egyptian brand
selling aviation-themed accessories (mainly keychains) plus a desk lamp and coasters.
Founded to bring aviation passion to Egypt for pilots, aspiring pilots and gift buyers.

- **Stack:** React 18 + TypeScript + Vite + Tailwind CSS + React Router 6 + Recharts + lucide-react
- **Data:** Supabase-backed and synced across all devices. `localStorage` is a local cache/offline mirror, NOT the source of truth. See "Data sync model" below.
- **Payments:** Cash on Delivery only (Egypt).
- **Deploy target:** Cloudflare Pages (build `npm run build`, output `dist`, `public/_redirects` handles SPA routing).

## Run / build
```bash
cd pilotgear-store
npm install
npm run dev      # http://localhost:5173
npm run build    # type-checks + builds to /dist
```
Preview tooling: `.claude/launch.json` defines server name `pilotgear` (runs dev with `--prefix pilotgear-store`).

## Architecture / key files
| Path | Purpose |
|---|---|
| `src/data/types.ts` | All TS interfaces (Product, Variant, Order, AnalyticsEvent, PageBlock, StoreSettings…) |
| `src/data/seed.ts` | Seed data: collections, all products, homepage blocks, settings. **Edit products/prices here.** |
| `src/data/store.ts` | localStorage store engine + React hooks (`useStore`, `useCart`), cart, orders, analytics, admin auth. **`KEY`/`CART_KEY` version strings live here.** |
| `src/App.tsx` | Routes. Admin is lazy-loaded. Tracks page_view on route change. |
| `src/components/storefront/StoreLayout.tsx` | Header (nav + mobile drawer), footer, announcement bar |
| `src/components/storefront/blocks.tsx` | Homepage block renderer (hero, marquee, valueProps, collections, featured, banner, giftCta, testimonials, newsletter) |
| `src/components/storefront/ProductCard.tsx` | Product card (variant products → "Choose options") |
| `src/pages/*` | Home, Shop, CollectionPage, ProductPage, Cart, Checkout, OrderConfirmation, About, Contact, Policy, NotFound |
| `src/admin/AdminApp.tsx` | Admin routes + auth gate |
| `src/admin/pages/*` | Dashboard (analytics), Products, ProductEditor, Collections, Orders, Inventory, PageBuilder, Settings |
| `src/admin/analytics.ts` | Metrics/funnel/top-product computation from events + orders |

## Data sync model (READ THIS before touching `store.ts`)
Supabase project `ecpeubpedcvxhwdbunzm` (`STORE_ID = 'main'`). Three tables, all with
public RLS policies (anon key, no auth — single-admin store):
- **`store_data`** (one row, `id='main'`): products, collections, homepage blocks,
  settings, saleLogs — as one JSONB blob. This is the source of truth for the catalog.
- **`orders`**: one row per order. Created on checkout; status/delete from admin.
  RLS has INSERT/SELECT/UPDATE **and DELETE** policies (DELETE was missing originally,
  which silently blocked order deletion — fixed).
- **`events`**: analytics events (page_view, product_view, add_to_cart, begin_checkout,
  purchase) from **every visitor browser**. This is what makes the dashboard funnel /
  conversion / traffic real and cross-device, not just the admin's own browser.

How `store.ts` keeps it consistent:
- `setData()` → mutates in memory, sets a persisted **`dirty`** flag, writes a slim
  localStorage cache (events excluded to avoid quota), debounced push to `store_data`.
- Pushes are **serialized** (`flushToSupabase`): concurrent calls share one in-flight
  promise + result, so "Save All" never reports a false failure.
- Pull/echo arbitration uses a numeric **`lastSyncedAt`** high-water mark (NOT timestamp
  strings). A pull is skipped while `dirty` so it can't clobber unsaved edits.
- Events are **batched** to `events` (keepalive beacon on pagehide) and pulled back into
  `data.events` (last 90 days) on load / 30s poll / focus. Events are NOT in localStorage.
- "Clear orders & analytics" and "Factory reset" wipe the `orders`/`events` tables
  server-side too (else a poll would resurrect them).

## IMPORTANT: data/seed versioning
Returning browsers cache `store_data` in `localStorage` under `KEY`, and Supabase holds
the live blob. To roll out a `seed.ts` change to existing stores you must bump the
version (`KEY`/`CART_KEY` in `store.ts` + `version` in `buildSeed()`) **and** the owner
must Factory reset (or the blob in Supabase must be updated), since the remote row is the
source of truth. Current `KEY`/`CART_KEY`: **v10**; `buildSeed().version`: **8**.

## Brand identity (exact)
- Navy (primary): `#0E3A5C`; navy-deep `#0A2C46`
- Gold (accent): `#EBB63F`
- Fonts: Poppins (headings, `font-head`), Inter (body)
- Logos in `public/brand/`: `logo.png` (navy/gold, trimmed), `logo-white.png` (for dark bg, generated by recolouring), `favicon.png`
- Source assets live in the parent folder `../` (Logo/, Product images/, AI Images/).

## Products & collections (current)
- **2 collections:** `Keychains` (id `col-pilot-life`, slug `keychains`) and `Home & Desk` (slug `home-desk`).
  (An "Aircraft Models" collection was removed; all keychains were merged into Keychains.)
- **10 keychains** (all 260 EGP, compare-at 320): Airbus A330, Cessna 172, AV8R, Captain, Eat Sleep Fly, I Love Flying, Runway, Pilot, Mini Airplane, Remove Before Flight.
- **Home & Desk:** Airbus A320 Desk Lamp (750), Flight Instrument Coasters (400).
- **Variants:**
  - Airbus A330 → Grey / Red (functional selector; gallery shows combined image because no clean single-colour photo exists).
  - Remove Before Flight → Red / Black / Blue / Yellow (image swaps per colour; each has a clean single-colour photo).
- **Best sellers** (badge only): Pilot Keychain + Remove Before Flight.
- **Shipping:** flat **70 EGP**, free over 800 EGP.

## Product images
All product images were standardised through a Python/PIL pipeline onto one uniform
background (`#F6F8FA`): composite transparent PNG → recolour near-white to bg → trim to
content → centre on a square canvas (900×900). Output as `public/products/<slug>/1.png`, `2.png`, …
- The pilot keychain source photos had a red **"REMOVE BEFORE FLIGHT" corner watermark** — removed by using the clean `dqvcfiir` photo and erasing the corner on others.
- To regenerate, re-run the PIL pipeline against `../Product images/` (prefer `No BG`/`Photoroom`/`No WM` transparent sources).

## Content rules (per the owner)
- **No em dashes** anywhere on the site. Use commas, colons, or hyphens.
- **No fake marketing**: no invented star ratings, review counts, or "X+ happy customers". Product star ratings are NOT displayed.
- **Real reviews only** (testimonials block): Habiba Khaled "Hi, El order bgd tuhfaa w el material hlwa gdn 💗💗" (Pilot Keychain); Yassin "Amazing 😍" (Pilot Keychain).
- Messaging targets **all aviation lovers**, not only pilots (e.g. value prop "For Aviation Lovers", not "By Aviators").

## Contact / settings (in `seed.ts` `seedSettings`)
- Email only: **pilotgeareg@gmail.com** (no phone, no WhatsApp — both removed from footer/contact).
- Instagram: `pilotgear.eg`
- Admin password: `pilotgear` (changeable in Admin → Settings).
- Currency: EGP.

## Admin dashboard (`/admin`, password `pilotgear`)
Dashboard (revenue/conversion/page-view analytics + funnel + top products + low-stock),
Orders (status workflow), Products (full CRUD + image upload + specs/variants),
Collections, Inventory (stock adjust + value), **Page Builder** (visual homepage block
editor with live desktop/mobile preview), Settings (store info, shipping, password,
JSON export/import, factory reset). Dashboard has a "Load demo data" button (only on an
empty store) to preview analytics.

## Known constraints / gotchas
- Mobile nav drawer must be rendered **outside** `<header>` — the header uses
  `backdrop-blur`, and a `backdrop-filter` ancestor re-anchors `position:fixed` children,
  which previously broke the full-screen drawer overlay. Keep it a sibling of `<header>`.
- Variant image swapping relies on `variant.image`; A330 variants intentionally have no
  per-colour image (no clean source), so the selector is functional only.
- Orders, catalog and analytics are synced via Supabase (see "Data sync model"), so they
  are consistent across devices. `localStorage` is only a cache; never treat it as the
  source of truth or assume an edit is saved just because it shows locally.
- The `events` table receives an insert per visitor action — fine for this store's
  volume. If traffic grows large, consider server-side aggregation instead of pulling
  raw events into `data.events`.
- "Load demo data" sets an in-memory `previewMode` that pauses remote pulls so the demo
  isn't wiped; it does NOT write to Supabase and is gone on refresh.
